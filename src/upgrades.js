// Placeholder for the legacy upgrade script, kept so existing connections' upgrade index stays aligned.
export const UpgradeScripts = [
	() => ({ updatedConfig: null, updatedSecrets: null, updatedActions: [], updatedFeedbacks: [] }),
]
